<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="wayfarer.bluetoothclienttest.MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/editText"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send"
        android:id="@+id/send_message"
        android:layout_marginTop="27dp"
        android:clickable="true"
        android:enabled="true"
        android:onClick="onSend"
        android:nestedScrollingEnabled="false"
        android:layout_below="@+id/editText"
        android:layout_alignEnd="@+id/editText" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stop Discovering"
        android:id="@+id/stop_discovering"
        android:layout_alignTop="@+id/send_message"
        android:layout_alignParentStart="true"
        android:onClick="onStopDiscovering" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reconnect"
        android:id="@+id/reconnect"
        android:layout_alignTop="@+id/stop_discovering"
        android:layout_centerHorizontal="true"
        android:onClick="onReconnect" />
</RelativeLayout>
